SoftAP
------
Library to make the ESP32 behave as a WiFi access point.

To use this copy the SoftAP directory into your library directory then include SoftAP.h. When you call ConnectWiFi() it will configure the ESP32 as a WiFi access point and print the details to the serial monitor.