# mdnslookup

_Note that the Linux app is currently untested. All volunteers to test it are welcome._

This is a Linux app to send mDNS A record requests. The syntax is:

```
./mdnslookup <target_name>
```

### Building the app

To build the app use:

```
gcc -o mdnslookup mdnslookup.c
```
